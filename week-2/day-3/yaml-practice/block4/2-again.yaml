apiVersion: batch/v1
kind: Job
metadata:
  name: parallel-batch-job
spec:
  parallelism: 6
  completions: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - image: busybox
          name: it-could-be-anything
          command: ['sh', '-c', 'ls -la']
