apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: web
    version: "1.0"
  name: web-v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
      version: "1.0"
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: web
        version: "1.0"
    spec:
      containers:
      - image: nginx:1.21
        name: nginx
        ports:
          - containerPort: 80

