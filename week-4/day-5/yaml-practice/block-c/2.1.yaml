apiVersion: v1
kind: Service
metadata:
  labels:
    app: inventory-db-service
  name: inventory-db-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: inventory-db-service
  type: ClusterIP
status:
  loadBalancer: {}
